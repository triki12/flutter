import 'package:flutter/material.dart';

class EmpruntPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(
      title: Text('Liste Emprunts'),
      centerTitle: true,
      backgroundColor: Colors.red,
    ),
  );
}
